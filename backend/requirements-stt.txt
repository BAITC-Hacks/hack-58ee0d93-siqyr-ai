# STT на CPU (модель Alibi: wav2vec2 + CTC, TorchScript).
# Установка: pip install --extra-index-url https://download.pytorch.org/whl/cpu -r backend/requirements-stt.txt
torch>=2.4
numpy>=1.26
soundfile>=0.12
onnxruntime>=1.18
huggingface_hub>=0.24
# Диаризация (нужен HF_TOKEN и принятие условий модели на Hugging Face):
# pyannote.audio>=3.3
