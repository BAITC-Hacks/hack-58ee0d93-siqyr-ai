# STATUS

Только дописываем: `- [HH:MM] <имя> done: ... | next: ... | gotchas: ...`

- [14:05] Codex planning done: заполнены PLAN/STACK/CRITERIA/CONTRACT/DATA/DEMO/README_OUTLINE/prompts и согласованы AGENTS/ROLES/ARCHITECTURE/AI_USAGE/DECISIONS; учтены $50 на API-ключ, $50 Brev, API synthetic-dev → local | next: команда запускает полосы по prompts.md; real E2E к 15:30, freeze 16:15 | gotchas: только документация; schemas.py и продуктовый код не менялись; JSON синтаксис/refs/required проверены, git diff --check пройден; полная metaschema-валидация не выполнялась; аудио-проверка спорных мест, модели, hardware Brev и согласие организаторов ещё предстоят. Никаких model downloads, API/GPU расходов, commit/push в ходе планирования.

- [13:59] Codex done: подготовлен PR frontend → main, приложение перенесено в frontend/ с сохранением backend и infra, npm ci, npm run build и Docker-сборка фронтенда проходят | next: review PR и отдельное подключение API/SSE/ИИ | gotchas: данные фронтенда пока только в IndexedDB; запись с физического микрофона не проверена; установленный Docker Compose не принимает существующий объект env_file из main.
- [14:05] Codex done: JWT-вход, роли и изоляция департаментов, валидатор токенов Keycloak, одноразовый challenge ЭЦП с проверяющим адаптером; 34 теста проходят | next: подключить UI, realm заказчика и внутренний CMS-верификатор | gotchas: без JWT_SECRET и bootstrap-пользователя API не стартует; ЭЦП отключена без ECP_VERIFY_URL
- [14:46] Meiirlan done: CONTRACT v0.2 на mock — PUT proposal с revision, approve с expected_revision/idempotency/immutable snapshot, серверная проверка evidence, GET audio с Range, stages в SSE, дата совещания без подстановки today, LLM_PROVIDER=local по умолчанию; 46 тестов проходят | next: ACK Alibi по полям и runner.py/engine.py, ACK Nurdaulet по экспорту, затем real STT/LLM | gotchas: не закоммичено; старая БД с NOT NULL meeting_date не примет запуск без даты — использовать новый DATA_DIR; в том же checkout параллельно появились profile_models.py (не мои правки).
- [14:50] Codex done: GET/PATCH профиля, настройки языка/темы/уведомлений, смена пароля с отзывом старых локальных JWT, загрузка/чтение/удаление приватного аватара; 49 backend-тестов проходят | next: подключить экран профиля во frontend | gotchas: AUTH_MODE=disabled разрешает только чтение профиля; корпоративный пароль меняется в Keycloak
- [14:57] Meiirlan done: упаковка — scripts/setup.sh (venv, lock, .env, npm ci, --with-stt, --download-models + manifest), scripts/run_local.sh (--offline, preflight, проверка портов, 1 worker), scripts/preflight.py, demo_e2e (--file, --mode, DOCX+PDF, expected_revision), backend/requirements.lock.txt, README быстрый старт; API: 503 при отсутствии весов/LLM endpoint, 429 при очереди > MAX_QUEUE, /health ready+problems | next: real STT/LLM от Alibi, прогон setup/run на Mac демо | gotchas: lock снят с Python 3.14/Windows — на Mac 3.12 при ошибке setup.sh --no-lock; ffmpeg/ollama на этой машине нет, real-путь не проверен; прогон без Wi-Fi физически не делали — mock-путь внешних вызовов не делает.
- [15:08] Meiirlan done: документация приведена к коду cc4d99b — CONTRACT (шапка, таблица маршрутов с PUT proposal/audio/503/429, статус v0.2, «Не реализовано», коды ошибок), BACKEND_HANDOFF переписан под v0.2, AGENTS/CRITERIA/handoff без устаревших «schemas v0.1» и «scripts не существуют» | next: engine.py/runner.py от Alibi, ACK Nurdaulet по экспорту, фикс assignee=null в pipeline.execute | gotchas: утверждение поручения с assignee=null роняет запуск в error (runner_mock.execute → Excerpt(recipient=None)); excluded получают выдержки; лимит 10 минут не проверяется; в checkout незакоммиченные /api/runs/recordings|chunks|finish (чужая работа в процессе) — в CONTRACT не внесены.
- [15:11] Codex done: режим «Записать звук» создаёт серверный run, передаёт секундные WebM-чанки по ходу записи, завершает через /finish и открывает карточку встречи с SSE-этапами; контракт и решение обновлены | next: интеграция серверного черновика/approval в карточку; проверка физического микрофона | gotchas: 21 API-тест прошёл, frontend build прошёл; UI микрофона не проверен на устройстве; в рабочем дереве есть параллельные изменения других задач.
- [15:40] Meiirlan done: интеграция Jira — backend/app/jira.py, `POST|GET /api/runs/{id}/jira`, таблица jira_issues (идемпотентность), активный спринт по JIRA_BOARD_ID, PDF вложением, `scripts/demo_e2e.py --jira`; 5 тестов на mock-транспорте, всего 61 проходит | next: токен Jira в .env и смоук на syqir.atlassian.net, кнопка «Создать задачи в Jira» во frontend (Nurdaulet) | gotchas: на реальной Jira ещё не проверено; Cloud только с JIRA_ALLOW_CLOUD=1 и сценарной записью; ссылки на аудио ведут на PUBLIC_API_URL (localhost работает только на ноутбуке демо).
- [15:35] Meiirlan done: Jira проверена на syqir.atlassian.net (Cloud, JIRA_ALLOW_CLOUD=1, mock-сэмпл): 4 задачи в активном спринте, due date, сказанный срок и цитаты в описании, PDF вложением, повтор без дублей; время утверждения в UTC+5 | next: кнопка во frontend, JIRA_USERS на участников команды, очистить доску перед демо | gotchas: тесты раньше брали JIRA_* из .env и создали мусорные SCRUM-9…16 — conftest теперь обнуляет JIRA_*; у mock-сэмпла нет аудио, поэтому ссылки ▶ на секунды появятся только на своей записи.
- [15:54] Meiirlan done: явный приём форматов записи — backend/app/media.py (расширение + MIME + сигнатура WAV/MP3/AAC/M4A/MP4/MOV/3GP/OGG/Opus/WebM/MKV/FLAC/WMA/AMR/AIFF/AVI/MPEG), 415 до записи на диск, хранение с расширением фактического контейнера, `GET /api/formats`; 109 тестов проходят, smoke через uvicorn (WAV 201, PDF→.mp3 415, .docx 415), реальные mp3 совещаний с ID3 распознаются | next: Nurdaulet — режим «Загрузить файл» сейчас кладёт файл только в IndexedDB; отправлять его в `POST /api/runs` и брать accept из `/api/formats` | gotchas: другие контейнеры проверены только синтетическими заголовками (ffmpeg локально нет); потоковая запись `/chunks` по-прежнему только WebM, Safari (MP4) не поддержан.
- [16:07] Meiirlan done: фронт — «Загрузить файл» при VITE_API_URL отправляет запись в `POST /api/runs` (ApiRecordingGateway.upload, таймаут 10 мин), встреча сохраняется с backendRunId только после приёма файла сервером, карточка показывает этапы; accept/лимит и проверка файла — из `GET /api/formats`; 400/401/403/413/415/429/503 объяснены по кодам контракта (AxiosHttpClient не пропускает тексты сервера). Все API-вызовы теперь несут Bearer из сессии входа — без этого при AUTH_MODE=local загрузка и запись с микрофона получали 401. npm run check: 42 теста, сборка; интеграция реальным gateway против uvicorn с AUTH_MODE=local: WAV → run → awaiting_approval, PDF под .mp3 → 415, без токена → 401 | next: живые этапы на карточке при AUTH_MODE=local — EventSource не шлёт Bearer, нужен fetch-SSE или опрос GET | gotchas: клик-тест UI в браузере не делался (вход требует ввода пароля).
- [16:11] Meiirlan done: почтовые напоминания (сценарий 2) — backend/app/mailer.py, SMTP из .env (SMTP_HOST/PORT/SECURITY/USER/PASSWORD/FROM), NOTIFY_EMAILS {имя: email} + NOTIFY_CC (куратор в копии и адресат для поручений без адреса); одно письмо на адресата, текст+HTML, ссылка на карточку при PUBLIC_APP_URL; таблица email_deliveries — отправленное не повторяется, ошибка SMTP до 3 попыток; фоновый reminder_loop и POST /api/reminders/run (+ email-статистика), email в /api/health и /api/notifications; 114 тестов проходят, smoke на :8010 с локальным SMTP-приёмником — 2 письма ушли автоматически, повтор без дублей | next: SMTP в .env, проверка python -m backend.app.mailer <адрес>, NOTIFY_EMAILS на участников демо, DEMO_TODAY для показа просрочки | gotchas: внешний SMTP (Gmail) выносит текст поручений из контура — только сценарные записи; Gmail только с паролем приложения; просроченное напоминается ежедневно; выдержки (excerpt) по почте не отправляются.
- [16:31] Meiirlan done: быстрая проверка почты через Resend — только RESEND_API_KEY (SMTP_HOST пустой) включает SMTP-шлюз smtp.resend.com:465, user resend, отправитель onboarding@resend.dev; свой SMTP_HOST важнее ключа; ошибка отказа получателя показывает ответ сервера; вход на Resend с ключом из .env проверен без отправки, 124 теста проходят | next: python -m backend.app.mailer <адрес владельца Resend>, NOTIFY_CC=<тот же адрес> для демо | gotchas: без своего домена Resend доставляет только на адрес владельца аккаунта; Resend — внешний сервис, только сценарные записи.
