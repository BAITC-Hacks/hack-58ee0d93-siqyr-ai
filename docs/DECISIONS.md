# DECISIONS

- [13:30] Кейс: «Система автопротоколирования совещаний с фиксацией поручений» (docs/TASK.md).
- [13:30] STT: своя модель Alibi `alibiserikbay/kazakh-russian-mixed-stt` (Apache-2.0, публичная, обучена до хакатона), `asr/rukk`, CPU, жадный CTC. KenLM и Whisper — в cut list.
- [13:30] Закрытый контур: STT только self-hosted; LLM через OpenAI-совместимый клиент с `LLM_BASE_URL` (vLLM + gpt-oss на Brev). Использование OpenAI API на синтетике — ждём ответа организаторов.
- [13:30] Контракт: propose() получает готовый транскрипт; STT и диаризация — отдельный слой backend/stt; needs_approval и final эмитит бэкенд.
- [13:30] Вход MVP: загрузка файла. Бот для Teams/Zoom/Meet и live-поток — в cut list.

- [13:59] Frontend: React + TypeScript + Vite в frontend/; Mantine — основной UI, Tailwind — точечные utilities без Preflight (выбор пользователя). Mantine Form и overlays выбраны для форм, фокуса и диалогов; React Router — для маршрутов и защиты активной записи; Dexie/IndexedDB — для локальных данных и медиа; docx — для клиентского DOCX; lucide-react и локальный Inter — для общих иконок и типографики. PDF доступен через печать браузера. API/SSE и интеграция ИИ остаются отдельной работой; CONTRACT.md и backend/shared/schemas.py сохранены.
- [2026-09-23] Авторизация: локальный JWT HS256 и scrypt-хэши паролей; `local`, `hybrid`, `keycloak` выбираются окружением. Keycloak access token валидируется по JWKS, issuer и audience; внешний `sub` заранее связывается с локальным пользователем. Корпоративные группы не назначают права автоматически до согласования маппинга с заказчиком.
- [2026-09-23] Права: организация → департамент → membership с ролью. Каждый запуск принадлежит департаменту; API фильтрует списки и проверяет ID маршруты, SSE и экспорт. Старые данные относятся к департаменту `default`.
- [2026-09-23] ЭЦП: NCALayer подписывает одноразовый challenge в браузере; backend выдаёт токен только после ответа внутреннего доверенного сервиса проверки CMS/сертификата/отзыва и совпадения payload. Пока сервис не указан, вход по ЭЦП закрыт. Для фронтенда потребуется клиент NCALayer (новая зависимость, если выбран npm-пакет).

## Уточнения плана после чтения реального состояния

- Пользователь запросил planning-only: обновлены документы и AGENTS, продуктовый код/схемы/requirements не изменены. Целевые команды ещё должны быть реализованы и проверены.
- Две рубрики по 100 баллов сохранены отдельно. Последняя пользовательская таблица определяет структуру защиты; DOCX определяет требования и отдельный акцент на воспроизводимости. Официальный приоритет рубрик не подтверждён.
- Запись [13:30] о Brev как закрытом контуре уточняется: Brev уже настроен, но размещение там не автоматически разрешено. До ответа организаторов — только синтетика. Основной рабочий и demo путь — laptop local, без deployment.
- Текущий Mac M1 Pro/16GB, Nurdaulet сообщил M3 Pro, RAM неизвестна. Основная local LLM qwen3:4b, fallback1.7b; gpt-oss20b эксперимент при подходящей памяти. Модели/версии до freeze фиксируются manifest.
- Последнее уточнение бюджета: $50 на каждый предоставленный API-ключ (их число не подтверждено), $50 Brev. Разрешены GPU, тестирование и data synthesis. Сначала dev OpenAI на полностью выдуманных встречах, затем local через тот же adapter. Это заменяет прежнее предположение $150 Brev.
- Облачный OpenAI, OpenAI-совместимый локальный API и open-weight gpt-oss — разные способы использования. Зачёт hosted synthetic-dev по правилам нужно подтвердить у организаторов. Содержимое исходных встреч ни на одном dev этапе не отправляется.
- CONTRACT v0.2 — явно спроектированное расширение v0.1: nullable speaker/assignee, raw vs corrected text, Transcript/Speaker, evidence/review, revision/approval snapshot, audio/save endpoints. Пользователь поручил описать интерфейсы; перед кодовой миграцией Meiirlan сверяет изменения с Alibi. Код сейчас v0.1, все расхождения перечислены в CONTRACT.
- «16 поручений / 8 понятных / 8 review» — ручной текстовый разбор, не измеренное качество автоматического извлечения. Аудио-эталон спорных мест ещё предстоит создать. Базовые DOCX не считаются дословным ground truth.
- Сохраняем реализованный React/Mantine/Dexie/npm frontend, включая выбранные Nurdaulet библиотеки. Старый CSS-only план заменён фактическим main; многоагентная оркестрация/SDK, notifier и cloud deploy не нужны до freeze. Сохраняем propose/execute и реальные этапы.
- Approval обязателен для выбранного продукта, это командное решение; ТЗ само по себе не определяет такой API. execute не меняет утверждённые fields. Reminders из сценария2 — после ядра, отложенность раскрывается.
- При проверке часов было 14:01 Астана. Исполнительный график пересчитан на 14:05–16:15 (130 минут на человека), вместо уже прошедшего 13:45. Чекпоинт 14:15 теперь фиксирует fixture/контракт и начало моделей; полный сквозной real-путь остаётся целью 15:30. Не притворяться, что прошедшие блоки выполнены.

- Подготовка публикации плана: сохранены frontend и записи команды из main 7bae7c2; COORDINATION фиксирует единственного редактора общих файлов и личные handoff. Предложение использовать готовый клиентский экспорт из approved snapshot требует ACK Meiirlan/Nurdaulet; API/schema не изменены. Документы публикуются отдельной веткой, merge остаётся у Meiirlan.
- [14:40] Демо: `AUTH_MODE=disabled` по умолчанию — в UI нет логина, план Alibi не включает авторизацию в MVP. Код JWT/Keycloak/ЭЦП остаётся, включается через env. `/api/health` отдаёт `auth_mode`. CORS по умолчанию включает порт фронта 5174. В compose `env_file` в простом синтаксисе (нужен `cp .env.example .env`).
